diff -Naur a/libs/WindowManager/Shell/res/drawable/ic_desktop_pill_dots.xml b/libs/WindowManager/Shell/res/drawable/ic_desktop_pill_dots.xml
--- a/libs/WindowManager/Shell/res/drawable/ic_desktop_pill_dots.xml	1970-01-01 00:00:00.000000000 +0000
+++ b/libs/WindowManager/Shell/res/drawable/ic_desktop_pill_dots.xml	2026-01-01 00:00:00.000000000 +0000
@@ -0,0 +1,23 @@
+<vector xmlns:android="http://schemas.android.com/apk/res/android"
+    android:width="120dp"
+    android:height="30dp"
+    android:viewportWidth="120"
+    android:viewportHeight="30"
+    android:tint="?android:attr/colorControlNormal">
+
+    <path
+        android:fillColor="@android:color/white"
+        android:fillAlpha="0.5"
+        android:pathData="M45,15 m-4,0 a4,4 0 1,1 8,0 a4,4 0 1,1 -8,0" />
+
+    <path
+        android:fillColor="@android:color/white"
+        android:fillAlpha="0.5"
+        android:pathData="M60,15 m-4,0 a4,4 0 1,1 8,0 a4,4 0 1,1 -8,0" />
+
+    <path
+        android:fillColor="@android:color/white"
+        android:fillAlpha="0.5"
+        android:pathData="M75,15 m-4,0 a4,4 0 1,1 8,0 a4,4 0 1,1 -8,0" />
+
+</vector>
--- a/libs/WindowManager/Shell/src/com/android/wm/shell/windowdecor/DesktopModeWindowDecoration.java	2026-04-07 09:00:34.224064723 +0000
+++ b/libs/WindowManager/Shell/src/com/android/wm/shell/windowdecor/DesktopModeWindowDecoration.java	2026-04-07 09:03:20.329225160 +0000
@@ -63,7 +63,10 @@
 import android.os.Handler;
 import android.os.Trace;
 import android.os.UserHandle;
+import android.provider.Settings;
+import android.util.Log;
 import android.util.Size;
+import android.util.TypedValue;
 import android.view.Choreographer;
 import android.view.Display;
 import android.view.InsetsState;
@@ -75,6 +78,7 @@
 import android.view.WindowManager;
 import android.view.WindowManagerGlobal;
 import android.widget.ImageButton;
+import android.widget.ImageView;
 import android.window.DesktopExperienceFlags;
 import android.window.DesktopModeFlags;
 import android.window.TaskSnapshot;
@@ -444,6 +448,7 @@
         if (!applyTransactionOnDraw) {
             t.apply();
         }
+        updatePillStyle();
     }
 
     /**
@@ -2156,6 +2161,54 @@
                 + "}";
     }
 
+    private void updatePillStyle() {
+        if (mResult.mRootView == null) return;
+
+        try {
+            ImageButton handle = mResult.mRootView.findViewById(R.id.caption_handle);
+            if (handle == null) {
+                //Log.e("DesktopModeFix", "CRASH PREVENTED: Handle view is NULL!");
+                return;
+            }
+
+            int style = Settings.Global.getInt(
+                    mContext.getContentResolver(), "desktop_mode_pill_style", 0);
+
+            //Log.d("DesktopModeFix", "Applying Pill Style: " + style);
+
+            int padVert = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 16,
+                    mContext.getResources().getDisplayMetrics());
+            int padHorz = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 10,
+                    mContext.getResources().getDisplayMetrics());
+
+            switch (style) {
+                case 0:
+                    handle.setAlpha(0.0f);
+                    break;
+
+                case 2:
+                    handle.setAlpha(1.0f);
+                    handle.setBackgroundResource(android.R.color.transparent);
+                    handle.setImageResource(R.drawable.ic_desktop_pill_dots);
+                    handle.setPadding(0, 0, 0, 0);
+                    handle.setScaleType(ImageView.ScaleType.FIT_CENTER);
+                    break;
+
+                case 1:
+                default:
+                    handle.setAlpha(1.0f);
+                    handle.setBackgroundResource(android.R.color.transparent);
+                    handle.setImageResource(R.drawable.decor_handle_dark);
+                    handle.setPadding(padHorz, padVert, padHorz, padVert);
+                    handle.setScaleType(ImageView.ScaleType.FIT_XY);
+                    break;
+            }
+
+        } catch (Exception e) {
+            //Log.e("DesktopModeFix", "CRASH CAUGHT! Here is the error:", e);
+        }
+    }
+
     @VisibleForTesting
     static class CapturedLink {
         private final long mTimeStamp;
--- a/packages/SystemUI/res/values/dimens.xml	2026-04-07 09:00:34.224064723 +0000
+++ b/packages/SystemUI/res/values/dimens.xml	2026-04-07 09:02:34.573558624 +0000
@@ -464,10 +464,10 @@
     <dimen name="status_bar_icon_horizontal_margin">0sp</dimen>
 
     <!-- the padding on the start of the statusbar -->
-    <dimen name="status_bar_padding_start">4dp</dimen>
+    <dimen name="status_bar_padding_start">18dp</dimen>
 
     <!-- the padding on the end of the statusbar -->
-    <dimen name="status_bar_padding_end">4dp</dimen>
+    <dimen name="status_bar_padding_end">-4dp</dimen>
 
     <!-- the padding on the top of the statusbar (usually 0) -->
     <dimen name="status_bar_padding_top">0dp</dimen>
--- a/packages/SystemUI/src/com/android/systemui/shade/QuickSettingsControllerImpl.java	2026-04-07 09:00:34.224064723 +0000
+++ b/packages/SystemUI/src/com/android/systemui/shade/QuickSettingsControllerImpl.java	2026-04-07 09:02:29.618053221 +0000
@@ -765,7 +765,7 @@
         if (getExpanded()) {
             return onHeader || (yDiff < 0 && isTouchInQsArea(x, y));
         } else {
-            return onHeader;
+            return onHeader || (yDiff > mTouchSlop);
         }
     }
 
--- a/services/core/java/com/android/server/wm/DisplayRotation.java	2026-04-07 09:00:34.224064723 +0000
+++ b/services/core/java/com/android/server/wm/DisplayRotation.java	2026-04-07 09:02:40.882202031 +0000
@@ -549,6 +549,13 @@
         final int lastOrientation = mLastOrientation;
         @Surface.Rotation
         int rotation = rotationForOrientation(lastOrientation, oldRotation);
+        if (mUserRotationMode == WindowManagerPolicy.USER_ROTATION_LOCKED) {
+            rotation = mUserRotation;
+            ProtoLog.v(WM_DEBUG_ORIENTATION,
+                    "Forcing locked user rotation=%s (%d)",
+                    Surface.rotationToString(rotation), rotation);
+        }
+
         // Use the saved rotation for tabletop mode, if set.
         if (mFoldController != null && mFoldController.shouldRevertOverriddenRotation()) {
             int prevRotation = rotation;
